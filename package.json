{
  "name": "ajar-docs-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the Ajar Protocol spec, schemas, examples, registries, and conformance vectors to coding agents",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "ajar-docs-mcp": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "derive": "node --experimental-strip-types scripts/derive-content.ts",
    "test": "vitest run",
    "start": "node dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
