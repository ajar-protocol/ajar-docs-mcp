# AGENTS.md

AI coding agents: the org-wide contract in https://github.com/ajar-protocol/.github/blob/main/AGENTS.md applies fully here.

Repo-specific rules:

- Content derives from the `ajar` spec repo. Never hand-edit derived data.
- Pin and report the served `ajar_version` in generated indexes and responses.
- Retrieval must be deterministic; do not rewrite normative language.
- Validation explanations cite schema paths and spec clauses.
- Keep local and hosted transports behavior-equivalent.
- Do not add telemetry.
